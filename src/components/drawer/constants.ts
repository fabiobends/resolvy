/**
 * Duration (ms) of the slide-up animation when the drawer opens.
 */
export const SHOW_DURATION_MS = 300;

/**
 * Duration (ms) of the slide-down animation when the drawer closes.
 */
export const HIDE_DURATION_MS = 200;

/**
 * Distance (in dp) the panel starts below the screen before animating up.
 */
export const SLIDE_DISTANCE = 300;
