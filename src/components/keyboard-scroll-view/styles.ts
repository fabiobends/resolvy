import { StyleSheet } from "react-native";

import { Spacing } from "@/constants/theme";

/** Styles for KeyboardScrollView content container. */
export const styles = StyleSheet.create({
  content: {
    flexGrow: 1,
    padding: Spacing.large,
  },
});
